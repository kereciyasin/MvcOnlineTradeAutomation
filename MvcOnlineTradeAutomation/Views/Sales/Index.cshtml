@model List<MvcOnlineTradeAutomation.Models.SalesTransaction>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="container mt-4">
    <div class="d-flex justify-content-between align-items-center mb-3">
        <h2><i class="fas fa-shopping-cart me-2 text-primary"></i> Sales Transactions</h2>
        @Html.ActionLink("Create New Sale", "CreateSale", null, new { @class = "btn btn-primary" })
    </div>

    <table id="tbl1" class="table table-bordered table-striped table-hover">
        <thead>
            <tr>
                <th>ID</th>
                <th>Product</th>
                <th>Customer</th>
                <th>Employee</th>
                <th>Quantity</th>
                <th>Price</th>
                <th>Total Amount</th>
                <th>Date</th>
                <th style="width: 150px;">Actions</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@item.SalesTransactionID</td>
                    <td>@item.Product.ProductName</td>
                    <td>@item.Customer.FirstName @item.Customer.LastName</td>
                    <td>@item.Employee.FirstName @item.Employee.LastName</td>
                    <td>@item.Quantity</td>
                    <td>@item.Price.ToString("C")</td>
                    <td>@item.TotalAmount.ToString("C")</td>
                    <td>@item.Date.ToString("dd/MM/yyyy")</td>
                    <td>
                        @Html.ActionLink("Edit", "UpdateSale", new { id = item.SalesTransactionID }, new { @class = "btn btn-sm btn-warning" })
                        @Html.ActionLink("Details", "SalesDetails", new { id = item.SalesTransactionID }, new { @class = "btn btn-sm btn-info" })
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

<!-- Bootstrap CSS -->
<link href="/Content/bootstrap.css" rel="stylesheet" />

<!-- ✅ DataTables Bootstrap 4 CSS -->
<link href="https://cdn.datatables.net/1.10.25/css/dataTables.bootstrap4.min.css" rel="stylesheet" />
<link href="https://cdn.datatables.net/responsive/2.2.9/css/responsive.bootstrap4.min.css" rel="stylesheet" />

<!-- ✅ Font Awesome (ikonlar için) -->
<link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"
      integrity="sha512-SnH5WK+bZxgPHs44uWIX+LLJAJ9/2PkfG1x2KX2rS6ZlJj1fVvYpVZg6mZf4H4qgqJqk8bV1Zrj7G8C4hZ8bBw=="
      crossorigin="anonymous" referrerpolicy="no-referrer" />

<!-- jQuery -->
<script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>

<!-- ✅ DataTables JS (Bootstrap 4 uyumlu) -->
<script src="https://cdn.datatables.net/1.10.25/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.10.25/js/dataTables.bootstrap4.min.js"></script>
<script src="https://cdn.datatables.net/responsive/2.2.9/js/dataTables.responsive.min.js"></script>
<script src="https://cdn.datatables.net/responsive/2.2.9/js/responsive.bootstrap4.min.js"></script>

<script>
    $(document).ready(function () {
        $('#tbl1').DataTable({
            responsive: true
        });
    });
</script>
